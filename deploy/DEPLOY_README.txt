EdgeAI-ZU4EV Deployment Package
================================
Generated by Vivado 2020.1

Files:
  cifar10_accel.bit   — FPGA bitstream (load via fpga_manager / board_load_only.sh)
  cifar10_accel.hwh   — Hardware hand-off (address map reference)
  cifar10_accel.xsa   — Xilinx Support Archive (optional PetaLinux BSP input)

Verified board path (PetaLinux 2020.1, no PYNQ):
  1. Boot ALINX ACU4EV with PetaLinux 2020.1
  2. From host: python scripts/board_ssh_deploy.py   (BOARD_IP=<your_board_ip>)
     Or copy artifacts to /root/ and run:
       bash board_load_only.sh
       python3 board_infer.py          # FPGA (devmem + AXI-DMA)
       python3 cpu_baseline.py         # CPU baseline (after tflite_runtime install)
  3. CPU tflite_runtime: bash scripts/board_install_tflite_and_rerun.sh (from WSL)

Optional reference only: see README § Step 4 for `board_infer.py` / `cpu_baseline.py`.
